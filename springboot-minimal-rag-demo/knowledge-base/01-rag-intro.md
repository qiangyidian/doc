# 为什么需要 RAG

大模型并不真正理解最新业务文档，它只能根据训练时学过的公开数据进行生成。

如果企业内部有自己的产品手册、接口文档、制度文档和 FAQ，大模型本身并不知道这些内容。

RAG 的核心思路是：

1. 先把业务文档切分成小块
2. 再把每个小块转成向量写入向量数据库
3. 用户提问时，先从向量数据库里找相关片段
4. 再把相关片段和用户问题一起发给大模型生成答案

RAG 解决的是知识截止、私有数据和回答可追溯这三个问题。