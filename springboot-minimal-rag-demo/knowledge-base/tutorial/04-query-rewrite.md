# 查询改写

查询改写的目标不是替换用户原问题，而是在正式检索前补出更适合搜索的表达。

例如用户只提“为什么接口名检索不稳定”，改写后可以补出“接口名 字段名 关键词 检索”之类更利于关键词通道命中的查询。

在 RAG 里，查询改写通常要遵守两个原则：
1. 保留原始问题，不能丢掉用户真实意图。
2. 改写数量要克制，避免引入太多噪声查询。

如果改写失败，主链路应该直接回退到原问题，不能让问答接口整体失败。
