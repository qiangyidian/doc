# 混合检索

混合检索指的是把向量召回和关键词召回结合起来使用。

当用户问题里出现接口名、字段名、报错文本或者精确术语时，只做向量检索经常不稳定，因为 embedding 更关注语义接近，不一定保留字面命中。

在工程上可以先跑向量召回，再补一条关键词召回通道，最后用 RRF 做融合排序。这样既保留语义召回能力，也能覆盖“接口名检索不稳定”这类精确命中场景。

关键词通道在 PostgreSQL 中可以结合 ILIKE 和 pg_trgm similarity 来做，适合当前这个单库教程项目。
