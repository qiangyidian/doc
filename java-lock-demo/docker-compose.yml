version: "3.9"

services:
  redis:
    # 使用官方 Redis 7 镜像。
    image: redis:7
    container_name: java-lock-demo-redis
    ports:
      - "6379:6379"
    command:
      # 开启 AOF，避免本地练习时重启丢失过多数据。
      - redis-server
      - --appendonly
      - yes